This is a virtual personality, providing the default icons. 
It doesn't appear on the admin page but is used if user 
selection is enabled. User can also reset the persona if 
'Great Void' is chosen. 